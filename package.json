{
  "name": "crossy-road",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A voxel hop-across-the-road arcade game built from scratch with Three.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
